package kafka.rest.admin.infrastructure.routes

class Routes {
    companion object {
        const val RESOURCE_NAME = "name"
        const val TOPICS = "/topics"
        const val CONSUMER_GROUPS = "/consumer-groups"
    }
}
